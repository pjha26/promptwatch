# Verseodin — Senior Frontend Trial

A paid 1-day trial to ship two small prototype features. Independent of any production codebase. Mock data. Your build.

> **TL;DR:** **8 hours of build, 5 calendar days to deliver. Both features required. AI tools allowed and expected.** Full details in [`docs/GROUND_RULES.md`](docs/GROUND_RULES.md).

---

## Start here

1. Read this file end-to-end (5 min).
2. Read [`docs/GROUND_RULES.md`](docs/GROUND_RULES.md) — pay, deliverable format, evaluation rubric, do/don't list, submission.
3. Read [`docs/FEATURE_1.md`](docs/FEATURE_1.md) and [`docs/FEATURE_2.md`](docs/FEATURE_2.md) — what you're building.
4. Skim [`docs/FAQ.md`](docs/FAQ.md) for non-goals and clarifications.
5. Set up locally (below). Start building.

## Setup

This scaffold is **Next.js 15 (App Router) + TypeScript + Tailwind 3 + Recharts + Lucide**. You're free to swap things out if you have a strong reason — we evaluate the same way. The scaffold is a no-opinion baseline so you can start coding immediately.

```bash
git clone <your-fork-url>
cd verseodin-trial-2026
nvm use            # uses Node 22 per .nvmrc
npm install
npm run seed       # generates public/visits.json + public/monitoring-events.json
npm run dev        # http://localhost:3000
```

You should land on a placeholder index page with links to `/traffic` and `/actions`. Both routes are empty placeholders today — your job is to replace them with the two features.

### What the seed scripts produce

| File | Rows | Used by | Generated by |
|---|---|---|---|
| `public/visits.json` | ~100,000 `AiVisit` records over 90 days | Feature 1 (AI Traffic) | `npm run seed:visits` |
| `public/monitoring-events.json` | ~200 `MonitoringEvent` records over 60 days | Feature 2 (Action Centre) | `npm run seed:monitoring` |

Both are deterministic (seeded RNG). Re-running produces the same dataset. The fixed reference date used by both is **`2026-06-03`** — do not switch to `new Date()` if you edit the seeds.

The seeded JSON files are **`.gitignore`'d**. Each reviewer regenerates by running `npm run seed` after cloning.

### What you do NOT need to do

- No backend, no API server, no database.
- No auth, no login.
- No real third-party data — everything is mocked in the seed scripts.
- No design system / theming work. Tailwind defaults are fine.

## What you're building

Two routes in the existing scaffold:

### `/traffic` — AI Traffic Dashboard (~3h)

A single page showing which AI crawlers visited the site over 90 days, on which pages, at what volume. Stacked bar chart + Top Pages + Top Crawlers. Full spec: [`docs/FEATURE_1.md`](docs/FEATURE_1.md).

**Difficulty:** the seed produces **~100,000 visits**. Your chart must re-render in **<150ms after a legend toggle** on a mid-range laptop, and initial aggregation must complete in **<500ms**. Naive `Array.filter` inside `useMemo` keyed on legend state will not hit this. Read the eval signal at the bottom of `docs/FEATURE_1.md` carefully.

### `/actions` — Action Centre (~3h)

A triage queue of recommended actions with Accept / Dismiss + filters + Active/Dismissed tabs. State persists across reload. Full spec: [`docs/FEATURE_2.md`](docs/FEATURE_2.md).

**Difficulty:** the seed produces **~200 raw `MonitoringEvent`s of 4 event types** — *not* pre-baked `Action`s. **You write the derivation function** `deriveActions(events: MonitoringEvent[]): Action[]` that turns the raw events into 15–30 Action cards with sensible `type`, `severity`, and human-friendly copy. The UI renders the function's output. The eval signal lives in the cleanliness and extensibility of that derivation.

## Submission

When you're done — or when you hit the 8-hour cap, whichever comes first:

1. Push to a public repo OR add the reviewer as a collaborator on a private one.
2. Make sure `git clone && nvm use && npm install && npm run seed && npm run dev` works cleanly on a fresh machine.
3. Reply to the original email with the repo URL.
4. We confirm receipt within 24 hours and reply with feedback within 5 business days.
5. Payment goes out on confirmation of receipt, not contingent on outcome.

Full submission checklist + README requirements: [`docs/GROUND_RULES.md`](docs/GROUND_RULES.md).

---

## Layout of this repo

```
verseodin-trial-2026/
├── README.md                       # you are here
├── docs/
│   ├── GROUND_RULES.md             # pay, rubric, do/don't, submission
│   ├── FEATURE_1.md                # AI Traffic detailed task
│   ├── FEATURE_2.md                # Action Centre detailed task
│   └── FAQ.md                      # non-goals + clarifications
├── app/
│   ├── page.tsx                    # placeholder index
│   ├── traffic/page.tsx            # YOUR FEATURE 1 GOES HERE
│   ├── actions/page.tsx            # YOUR FEATURE 2 GOES HERE
│   ├── layout.tsx
│   └── globals.css
├── lib/
│   └── types.ts                    # shared type contracts — do not modify
├── scripts/
│   ├── seed-visits.ts              # generates public/visits.json
│   └── seed-monitoring.ts          # generates public/monitoring-events.json
├── public/                         # seeded JSON lands here (.gitignore'd)
├── package.json
├── tsconfig.json
├── next.config.ts
├── tailwind.config.ts
├── postcss.config.js
├── eslint.config.mjs
├── .prettierrc
├── .editorconfig
├── .nvmrc
└── .gitignore
```

## Questions before you start?

The brief is designed to be self-contained. Read it once end-to-end before asking. If something is genuinely ambiguous, reply to the original email — we'd rather clarify upfront than read a guess. Good luck.

---

## Action Centre Derivation Rules (Feature 2)

I implemented an extensible, array-based rules engine in `app/actions/lib/derivation.ts` rather than a hardcoded switch statement. Each rule defines how to match, aggregate, and map a specific `event_type` into an Action. This ensures adding a 5th event type is just configuration, not refactoring.

**1. Reddit Mentions (`reddit_competitor_mention`)**
- **Action Type**: `reddit` (Engage with the thread).
- **Aggregation**: 1:1. Each thread represents a distinct engagement opportunity.
- **Severity**: Based on total thread engagement (`upvotes + comment_count`). `High` >= 100, `Medium` >= 30, otherwise `Low`.

**2. Article Published (`article_published_with_competitors`)**
- **Action Type**: `outreach` (Pitch the author for inclusion).
- **Aggregation**: 1:1. Each article is a distinct PR opportunity.
- **Severity**: Based on `estimated_monthly_traffic`. `High` >= 20,000, `Medium` >= 5,000, otherwise `Low`.

**3. Citation Missed (`citation_missed`)**
- **Action Type**: `content` (Publish gap content targeting the prompt).
- **Aggregation**: Grouped strictly by identical `prompt`. The user shouldn't see 4 separate actions for the same prompt; they should see 1 action that says "You missed citations for this prompt across 4 queries."
- **Severity**: Driven by frequency of the missed prompt. `High` >= 3 occurrences, `Medium` == 2, `Low` == 1.

**4. Competitor Cited Instead (`competitor_cited_instead`)**
- **Action Type**: `outreach` (Investigate and reach out to the source to replace or join the competitor).
- **Aggregation**: 1:1.
- **Severity**: Driven by the competitor's rank/position. `High` <= 2 (top results are critical), `Medium` <= 4, otherwise `Low`.

**Stable IDs**: `Action.id` is deterministically generated by sorting the `source_event_ids` and applying a lightweight hash. This guarantees the same events will always yield the same IDs across reloads, making persistence rock-solid.
